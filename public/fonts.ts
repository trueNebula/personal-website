import { Exo, Geologica } from "next/font/google";

export const headingFont = Exo({
    weight: "700",
    subsets: ["latin"],
});
export const mainFont = Geologica({
    weight: "400",
    subsets: ["latin"],
});